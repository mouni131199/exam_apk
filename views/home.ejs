<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Exam App | Home</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #e3f2fd, #ffffff);
      font-family: 'Segoe UI', sans-serif;
    }

    .home-container {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 30px;
    }

    .card-custom {
      padding: 40px 30px;
      border-radius: 20px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.1);
      background-color: #fff;
      text-align: center;
      width: 100%;
      max-width: 500px;
    }

    .welcome-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #0d6efd;
      margin-bottom: 15px;
    }

    .lead {
      font-size: 1.1rem;
      color: #555;
      margin-bottom: 30px;
    }

    .btn-custom {
      width: 140px;
      margin: 10px;
      font-weight: 600;
      border-radius: 10px;
    }

    .btn-login {
      background-color: #0d6efd;
      color: white;
    }

    .btn-register {
      background-color: #198754;
      color: white;
    }

    .info-text {
      font-size: 0.95rem;
      color: #6c757d;
      margin-top: 15px;
    }

    .admin-note {
      font-size: 0.85rem;
      color: #dc3545;
      margin-top: 10px;
    }
  </style>
</head>
<body>

  <div class="home-container">
    <div class="card-custom">
      <h1 class="welcome-title">Welcome to Exam Portal</h1>
      <p class="lead">Prepare, attempt, and track your exam progress — all in one place.</p>

      <div>
        <a href="/auth/login" class="btn btn-custom btn-login">Login</a>
        <a href="/auth/register" class="btn btn-custom btn-register">Register</a>
      </div>

      <p class="info-text">
        Already a user? Click login.<br>
        New here? Start with register.
      </p>

      <p class="admin-note">
        ⚠️ Admins and Super Admins do <strong>not</strong> need to register. <br>
        Please use your assigned credentials to log in.
      </p>
    </div>
  </div>

</body>
</html>
<%- include('partials/footer') %>